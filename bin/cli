#!/usr/bin/env node 

'use strict';

const program = require('commander');
const {
  lookup
} = require('../index');
// var repl = require('repl');
// var r = repl.start('> ');
// var c = r.context ;

program
  .action(function () {
    lookup();
  }) 
